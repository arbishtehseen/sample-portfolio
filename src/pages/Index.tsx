import LoginCard from "@/components/LoginCard";

const Index = () => {
  return (
    <div className="login-bg flex items-center justify-center px-4 py-8">
      <LoginCard />
    </div>
  );
};

export default Index;
